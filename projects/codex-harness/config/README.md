# Project configuration boundary

This directory is reserved for human-facing configuration owned by this
project. Runtime kernel state and generated local configuration remain under
`.harness/`; no configuration is shared with another project or capability.
