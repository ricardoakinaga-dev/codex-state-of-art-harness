# P7.3-QB-1 — Quality Bar

Status: `PASS_WITH_LIMITATIONS`

Feature freeze: `P7_3_FEATURE_FREEZE`

| ID | Dimension | Target | Evidence method | Required | Validity |
| --- | --- | --- | --- | --- | --- |
| P73-01 | Authority | Phase 7.2 is identified as the authoritative prior packet; Phase 7/7.1 remain historical inputs | direct paths, hashes and closeout index | yes | prevents superseded evidence from becoming current by implication |
| P73-02 | Freeze | Every source/test change maps to a `P7.3-FINDING-*` or the evidence contract; no feature surface expands | plan, Git diff and finding map | yes | keeps final closure bounded |
| P73-03 | Risk semantics | High and Medium counts separately expose classified, actionable, promotion-blocking and closure states | `risk-semantics-spec.md`, JSON schemas and validator | yes | prevents a proven-unreachable High from being reported as actionable |
| P73-04 | Inventory | Current source/test coverage produces one exact record per residual branch with no branch exclusions | fresh branch coverage JSON and `medium-risk-inventory.json` | yes | ensures the classification set is complete for the reviewed bytes |
| P73-05 | Medium closure | Every Medium record has materiality, evidence, closure state and reason; material Medium records bind to separately reviewable proof; open actionable and promotion-blocking Medium are `0` | inventory, closure summary, `material-medium-proof.json`, tests/proofs and ledger | yes | material residuals cannot disappear behind a count change |
| P73-06 | Count consistency | Inventory, risk map, readiness, ledger and final-report count projections are identical | `risk-count-consistency.json` and validator exit `0` | yes | blocks semantic drift between machine and human reports |
| P73-07 | High/H-01 | `open_actionable_high = 0`, `promotion_blocking_high = 0`, and H-01 remains `CLOSED` | risk ledger, readiness and prior packet binding | yes | preserves the previous high-risk gate |
| P73-08 | Host | Host is pinned with observable path/version/config/fingerprints; current real cycle is `PASS` or waiver meets every Section 28 criterion | bootstrap manifest, cycle receipts or expiring waiver | yes | distinguishes host availability from candidate capability behavior |
| P73-09 | Security | Available scanners run; unavailable scanners remain `UNAVAILABLE` and are explicitly waived or block; no Critical/High finding remains | scanner inventory/report, waivers, static/dependency review | yes | absence of a tool is never represented as a security pass |
| P73-10 | Integrity | Candidate and verifier fingerprints remain stable; workspace, installed package and global state have zero mutation | receipts, before/after hashes and mutation checks | yes | binds the decision to exact project-local bytes |
| P73-11 | Regression | Full suite has at least 1,658 passing tests; line and branch coverage remain at least 80%; Ruff and strict mypy pass | fresh commands and artifacts | yes | catches cross-phase regressions and keeps the existing floor |
| P73-12 | Cross-phase | Phase 2, 3, 4, 5, 6, 7, 7.1 and 7.2 regressions, backend pilot and verifier local regression pass | fresh phase reports | yes | prevents a local closure from invalidating earlier contracts |
| P73-13 | Review | Exact manifest/attestation bind the reviewed HEAD and fresh independent read-only review returns `PASS` | manifest, attestation and reviewer report | yes | separates implementation from acceptance judgment |
| P73-14 | Decision | Promotion is `VERIFIED_CANDIDATE_WITH_LIMITATIONS` only when all promotion criteria and ledger invariants pass; broader authorization remains separate | promotion decision, readiness, final report and freeze marker | yes | avoids promotion-by-preference |

Required numerical targets:

- `critical_open = 0`
- `open_actionable_high = 0`
- `promotion_blocking_high = 0`
- `open_actionable_medium = 0`
- `promotion_blocking_medium = 0`
- line coverage `>= 80%`
- branch coverage `>= 80%`
- full test count `>= 1,658`, all passing

The phase does not claim `PRODUCTION_READY`, `SECURITY_APPROVED`,
`RELEASE_APPROVED`, `STABLE`, `AAA_VERIFIED`, universal or causal superiority,
all branches covered, exhaustive failure-path testing, syscall-level isolation
or full host causality.
