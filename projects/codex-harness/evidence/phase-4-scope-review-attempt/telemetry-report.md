# Telemetry report

The receipt lifecycle is `DISCOVERED → RESOLVED → AUTHORIZED → CONTEXT_PREPARED → INVOCATION_REQUESTED → HOST_ACKNOWLEDGED → EXECUTING → RESULT_RECEIVED → VERIFYING → VERIFIED → CLOSED`. The host emitted `32` bounded events, denied `0` approval requests and returned status `SUCCESS` after approximately `4.313` wall-clock seconds.
