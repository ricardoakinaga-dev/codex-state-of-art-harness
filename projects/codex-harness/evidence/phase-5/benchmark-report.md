# Benchmark report

`P5-BENCH-1` is a single local comparison labeled
`PILOT_COMPOSITION_EVIDENCE`. The native minimal baseline scored 34/100 with
8 recorded material evidence items and a 38 ms local navigation observation;
the final builder/review composition scored 87/100 with 4 items and 58 ms.

The observed deltas are +53 score points, four fewer evidence items and +20 ms
in this one navigation. The comparison is not causal proof, a production
benchmark, an SLO or evidence that the installed Skill alone caused the delta.
The machine-readable source is [`benchmark-summary.json`](benchmark-summary.json).
