# Phase 1 format and lint report

```text
$ .venv/bin/ruff format --check src tests
31 files already formatted

$ .venv/bin/ruff check src tests
All checks passed!
```

O conjunto Ruff cobre `E`, `F`, `I`, `B`, `UP` e `SIM` com line length 100.
