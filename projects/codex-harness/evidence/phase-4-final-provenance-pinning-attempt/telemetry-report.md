# Telemetry report

The receipt lifecycle is `DISCOVERED → RESOLVED → AUTHORIZED → CONTEXT_PREPARED → INVOCATION_REQUESTED → HOST_ACKNOWLEDGED → EXECUTING → RESULT_RECEIVED → VERIFYING → VERIFIED → CLOSED`. The host emitted `30` bounded events and denied `0` approval requests. The complete parsed protocol count was `38`, including `0` MCP events and `0` approval requests; it returned status `SUCCESS` after approximately `7.575` wall-clock seconds.
