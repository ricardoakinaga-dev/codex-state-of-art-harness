# Host-load causality report

The host observed a complete bounded turn for `phase4-safe-pilot`: exact discovery, ephemeral read-only thread, typed Skill input and turn completion. The event count was `30` and no correlated Skill-load event was exposed, so the causal status is `HOST_LOAD_UNOBSERVABLE`. This is `P4_LEVEL_B`, not Level C.
