# Telemetry report

The receipt records `DISCOVERED → RESOLVED → AUTHORIZED → CONTEXT_PREPARED → INVOCATION_REQUESTED → HOST_ACKNOWLEDGED → EXECUTING → RESULT_RECEIVED → VERIFYING → VERIFIED → CLOSED`. Host event count was 34; load causality remained unobservable; no approval was requested; no tool call, script, network, MCP, provider, credential or subagent operation was authorized.
