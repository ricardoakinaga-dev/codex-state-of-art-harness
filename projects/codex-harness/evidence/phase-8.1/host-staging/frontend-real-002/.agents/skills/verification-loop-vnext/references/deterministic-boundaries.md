Load when: a deterministic procedure or safety boundary must be selected.

Use only the declared procedure metadata and authorized project-local inputs.
The procedure reads identities, bounded metadata and evidence references and
returns an observation with one attempt. Enforce the context, reference,
procedure, time, report and evidence-record limits before continuing.

Shell, network, MCP, provider, credential, subagent, arbitrary interpolation
and workspace-write surfaces are denied. Reject absolute paths, traversal,
aliases and unapproved references. Stop on a blocking mismatch, stale input,
missing requirement, no progress, repeated failure or exhausted budget.
